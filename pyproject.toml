[project]
name = "poc-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "fastapi>=0.115.0",
    "pydantic>=2.0.0",
    "pytest>=9.0.2",
    "requests>=2.32.5",
    "uvicorn[standard]>=0.30.0",
]
